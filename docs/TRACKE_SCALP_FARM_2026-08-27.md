# Track E — 단타 팜 (Scalp Farm) 사전등록 명세 (2026-08-27)

> 명세 v1, Codex 1라운드 반영 확정본. 본 문서가 사전등록 원본이며 T0 이후 수치·규칙 변경 금지.
> 변경이 필요하면 신규 트랙을 별도 사전등록한다.

## TL;DR
**페이퍼 전용 10셀(5전략 × 2바스켓) 단타 팜.** 알파 탐색이 아니라 (a) 사용자 가시 단타
실험, (b) 실행 파이프라인 검증·비용 침식 관찰, (c) 상관된 다중 경로에서 승자가 자연
발생하는 현상(사후선택 함정)의 라이브 시연이 목적이다. **어떤 셀도 실거래·승급 후보로
선발 금지.** 판정은 사전 지정 3시점(T+30/90/180)의 공동 null 부트스트랩뿐이다.

## 1. 지위 (동결)
- **페이퍼 전용. 실주문·실자금 0. 승급 근거 사용 영구 금지** (Track B 방화벽 전례).
- 목적: (a) 사용자 가시 단타 실험 (b) 실행 파이프라인 검증·비용 침식 관찰
  (c) 상관된 다중 경로에서 승자가 자연 발생하는 현상(사후선택 함정)의 라이브 시연.
- 알파 탐색 아님. Track E 성과로 어떤 셀도 실거래·승급 후보로 선발 금지 —
  후보를 살리려면 별도 트랙을 신규 사전등록해 새 전방 데이터로 재검증.
- 명칭: "지갑" 금지 (H2 실제 지갑과 혼동) → **가상계정/셀 E01~E10**.

## 2. 셀 구성 (10 = 5전략 × 2바스켓, T0 동결)
- 바스켓 A = BTC/ETH/SOL (백테스트 검증 유니버스).
- 바스켓 B = T0 시점 봇 유니버스(거래량 상위)에서 BTC/ETH/SOL 제외 차상위 3종,
  **T0에 동결·이후 교체 없음**, 라벨 "OOD·미검증 코인셋".
- 전략 (기존 분석의 사전 고정 규칙, 신규 튜닝 금지 — 단 아래 '실행 교정'은 전 셀 일괄 적용):

  | 셀 | 전략 | 바스켓 | 고정 라벨 |
  |---|---|---|---|
  | E01/E02 | BRK24 (1h 채널 돌파 N=24) | A/B | 역사적 탈락 |
  | E03/E04 | BRK48 | A/B | 역사적 탈락 |
  | E05/E06 | BRK96 | A/B | 격자 1/8 선택 · Track D 중복 · 선택할인 |
  | E07/E08 | MR (24h SMA z>2 페이드) | A/B | 역사적 탈락 |
  | E09/E10 | RSI-DIV 4h (불단왕 규칙) | A/B | **미검증 가설 U1** |

- 규칙 원전: `lab/scalp_grid.py` (BRK/MR), `lab/rsi_divergence_test.py` (RSI-DIV).

## 3. 실행 교정 (성과 조회 전 전 셀 일괄 — Codex 지적 인과성 결함 수정)
1. BRK: 스탑·수량 산정에 **ATR[i−1]** (같은 봉 완성 ATR 사용 금지).
2. ATR의 TR은 **previous close** 기준 (swing.py 버그 재발 금지).
3. MR·RSI-DIV: 신호는 확정봉 종가로 계산, **체결은 다음 봉 시가** (신호봉 종가 체결 금지).
   BRK 돌파는 봉내 스탑주문 모델(터틀 전례: 돌파 레벨 체결 + 갭 악화) 유지.
4. 4h 봉: UTC [00,04,08,…) 정렬, 닫힌 1h봉 4개 전부 있을 때만 확정.
5. 누락 봉 구간 fail-closed(해당 셀 그 봉 무행동 + 경고 로깅). 보간 금지.
6. 워밍업(지표 초기화)은 T0 이전 봉으로만, **T0 이전 주문 생성 금지** (과거 재생 거래 금지).
7. 펀딩: 실제 정산 시각(00/08/16 UTC)별로 그 시점 보유 포지션에 적용.
8. 이벤트 유일키 (cell, symbol, strategy, bar_close, action) — 재실행 멱등.
9. 상태·원장 원자적 체크포인트 (임시파일→rename, 상태와 이력 동일 트랜잭션 순서).

## 4. 리스크 (셀당, 동결)
- 가상 자본 $10,000/셀 (총 $100,000 표기). 리스크 2%/포지션 (스탑거리 역산 사이징).
- 동시 최대 3포지션(=3코인). **그로스 캡 10x** + **포트폴리오 heat 6%** (신규 진입 차단 기준).
- 일손실 −5% 도달 시 당일 신규 진입 정지 (트리거이지 손실 상한 아님 — 문서·UI에 명시).
- 비용: 왕복 16bp (taker 6+슬립 2, 편도 8bp) + 펀딩. scalp_grid 모델 그대로.
- 폐지·데이터 단절: 마지막 유효가로 청산 후 해당 코인 슬롯 영구 공석 (교체 없음).

## 5. RSI-DIV (U1) 포함 절차 (동결)
- 실행 계약 완성(사이징·비용·정지·펀딩 통합) → 코드 동결 → **백테스트 1회 실행·결과
  문서 기록** → **결과와 무관하게 E09/E10 포함**. 좋다고 승격·나쁘다고 제외 금지.

## 6. 판정·표시 (동결)
- 대시보드: PAPER ONLY 배너(가상 $100,000 · 실제 주문/자금 0 · 승급 근거 사용 금지),
  E01~E10 **고정 순서** (성과순 정렬 금지), 셀별 고정 라벨 표시, 동일가중 팜 곡선,
  중앙값/IQR. 현재 최대 셀에 상시 "**사후 최대값 — 선택 금지**" 태그.
  트로피·우승자·초록 강조 금지. 지표: 수익률·비용누적·펀딩누적·turnover·gross·MDD·정지횟수.
- 행운 밴드(±2σ√t) **채택 안 함** (다중비교 무시 — 기각됨).
- 통계 판정은 사전 지정 시점만: **T+30 = 2026-09-26, T+90 = 2026-11-25, T+180 = 2027-02-23**
  (`lab/tracke_null.py` — 지금 동결): 10셀 동시 일별 gross 손익 벡터(비용 분리) 평균0 중심화 →
  동기화 stationary block bootstrap (블록 기하분포 평균 5일, 10,000경로, seed=20260827) →
  경로별 비용 재차감 → "10셀 최대 누적수익" 분포 → 관측 최대와 비교.
  허용 표현 2가지만: "최대값이 zero-edge 공동 null과 구별되지 않음" /
  "공동 null 상단(95%) 초과 — 엣지 입증 아님, 별도 전방 확인 필요".
- 구조적 방화벽: Track E 상태·이력이 승급/실거래 게이트 입력에 못 들어가는 구조 테스트.

## 7. 구현 (관례)
- `carrybot/aggressive/scalp_farm.py` (엔진 — 백테스트·라이브 동일 step(), 터틀 전례),
  `scalp_farm_runner.py` (1h 멱등 러너, 갭 fail-closed, T0 동결 초기화),
  `lab/tracke_null.py` (판정), 대시보드 섹션, `docs/TRACKE_SCALP_FARM_2026-08-27.md` (본 명세).
- 상태 `logs/tracke_state.json`, 이력 `logs/tracke_history.csv` (+셀별 원장 `logs/tracke_ledger.csv`).
- 스케줄: GH Actions 매시 (기존 워크플로 관례). 캔들: 기존 bybit_client/candle_fetcher 재사용.
- T0 = 첫 러너 실행 시각 기록, 바스켓 B 그 시점 동결.

---

## 8. U1 사전 기록 — 결과와 무관하게 E09/E10 포함

§5 절차에 따라 원본 백테스트를 코드 동결 상태에서 1회 실행하고 결과를 여기 기록한다.
**이 결과는 E09/E10 포함 여부에 영향을 주지 않는다** (좋아도 승격 금지, 나빠도 제외 금지).
라벨은 "미검증 가설 U1" 그대로 유지한다.

### 실행 조건 (재현 가능)
- 실행일: 2026-08-27, 커맨드: `.venv/bin/python lab/rsi_divergence_test.py` (저장소 루트)
- 코드: `lab/rsi_divergence_test.py` 무수정 원본, SHA-256
  `965c0556cda961ff9ad3b5e129907472b5b802343c33b63ce7760a0ba7b4451f`
  (Track E 동결 커밋에 포함해 버전관리로 고정)
- 데이터: `lab/frozen/perp_1h.parquet` (동결 2026-08-24T02:49:41Z,
  SHA-256 `c06a3301457dfec8f68b184e1f8ac8797acc49874fa96c409a6a57e6743ebac0`)
  - BTC 1h: 2021-01-01 ~ 2026-08-24 02:00 UTC (49,467봉), ETH 1h: 2021-03-15 ~ 동일 (47,715봉)
- 비용: 왕복 16bp (스크립트 상수 `RT_COST=0.0016`), 사이징·펀딩·정지 없음 (거래당 단순수익 합산)

### 결과 (원본 출력 그대로)

| | BTC | ETH |
|---|---|---|
| 거래 수 | 269건 (48건/년, 2021-01-10~) | 253건 (47건/년, 2021-03-22~) |
| 승률 (비용 후) | 37.5% | 37.9% |
| 평균 수익/건 | −0.138% | +0.046% |
| **누적 순수익 (단순합)** | **−37.1%** | **+11.6%** |
| 롱 | 121건, 승률 37% | 114건, 승률 43% |
| 숏 | 148건, 승률 38% | 139건, 승률 34% |
| 연도별 누적 | 2021 −48.2% · 2022 +56.9% · 2023 −61.8% · 2024 −21.9% · 2025 +16.4% · 2026 +21.5% | 2021 −40.9% · 2022 +41.6% · 2023 −44.1% · 2024 +4.4% · 2025 +103.8% · 2026 −53.2% |

### 사전 기록 해석 (판정 아님)
- 비용 차감 후 BTC 음수, ETH 소폭 양수 — 연도별 부호가 심하게 교차하며 "승률 높다"는
  원 주장(불단왕)은 두 심볼 모두 기각 수준(승률 ~38%, 2R 구조 감안해도 손익분기 ~33% 근처).
- 결론 없음: 절차상 **결과와 무관하게 E09/E10 포함**되며, 이 수치는 라이브 전방 성과와
  대조하기 위한 사전 기준선으로만 쓴다.

---

## 9. RSI-DIV 이식 대조 체크리스트 (lab/rsi_divergence_test.py → scalp_farm.py)

전제: 실행 교정(§3)·사이징·비용·정지·펀딩 통합은 엔진(scalp_farm.py) 공통 계층이 충족.
아래는 **원본 규칙 자체가 정확히 옮겨졌는지** 엔진 코드와 1:1 대조할 항목이다.
원본과 의도적으로 다른 항목은 #1·#11(교정)과 #13·#17(교정 파생 — 아래 '확정 사항'에서
결정 완료)이며, #15는 원본 유지로 확정, 나머지는 원본과 완전 일치해야 한다.

| # | 항목 | 원본 규칙 (lab 파일 근거) | 엔진 요구 | 원본 대비 |
|---|---|---|---|---|
| 1 | 4h 봉 구성 | 1h→4h 리샘플 open=first/high=max/low=min/close=last (L33-34) | 동일 집계, 단 UTC [00,04,08,…) 정렬 + 닫힌 1h봉 4개 전부 있을 때만 확정 (§3-4), 결측 시 무행동 (§3-5) | 교정 (원본 dropna는 부분 봉 허용) |
| 2 | RSI(14) | Wilder: `d.clip` 상/하단, `ewm(alpha=1/14, adjust=False)`, dn=0→NaN (L14-19) | 동일 재귀식 (증분 갱신 시 초기화·NaN 처리 동일) | 일치 |
| 3 | ATR(14) | TR = max(h−l, \|h−prev_close\|, \|l−prev_close\|), `ewm(alpha=1/14, adjust=False)` (L21-24) | 동일 — 원본이 이미 prev_close 기준 (§3-2는 이를 성문화한 것, swing.py식 현재 close 사용 금지) | 일치 |
| 4 | 피벗 정의 | K=2: j=i−K가 [j−2, j+2] 5봉 창의 극값(low는 min, high는 max, `==` 비교라 동률도 피벗)이면 i 시점에 확정 (L40-47) | 동일 (확정 지연 2봉, 동률 처리 포함) | 일치 |
| 5 | 피벗 저장값 | (j, 극값 가격, **RSI[j]**) — RSI는 피벗 봉의 값 (L44, L47) | 동일 (확정 시점 i의 RSI 아님) | 일치 |
| 6 | 다이버전스 조건 | 최근 두 피벗 (j1,·),(j2,·): j2−j1 ≤ 50 (4h봉 기준) **그리고 j2 == i−K** (신규 확정 봉에서만 신호) (L70, L75) | 동일 — 신선도 조건(j2==i−K) 누락 시 과거 다이버전스가 재발화하므로 필수 | 일치 |
| 7 | 방향 판정 | 상승: p2 < p1 **and** r2 > r1 → 롱 / 하락: p2 > p1 and r2 < r1 → 숏, 전부 엄격 부등호 (L70, L75) | 동일 (≥/≤ 금지) | 일치 |
| 8 | 신호 우선순위 | 같은 봉에 롱·숏 신호 동시 성립 시 **롱 먼저 평가 후 continue** (L68-72 → L73) | 동일 순서 보장 (또는 명시적 동시신호 규칙을 원본과 같게: 롱 우선) | 일치 |
| 9 | 포지션 수 | 심볼당 1포지션, 보유 중 신규 신호 무시 (L51 `if pos is not None … continue`), 피라미딩 없음 | 동일 + 셀 공통 캡(3포지션·heat·gross)은 상위 계층 (§4) | 일치 |
| 10 | ATR 유효성 게이트 | `not np.isnan(a.iloc[i])` 아니면 신호 폐기 (L70, L75) | 동일 (워밍업 미완 시 무진입) | 일치 |
| 11 | 진입 체결 | 신호봉 종가 `e = close[i]` (L71, L76) | **다음 봉 시가 체결** (§3-3) | 교정 |
| 12 | 스탑 | 롱: p2 − 0.5×ATR / 숏: p2 + 0.5×ATR — **피벗 극값 기준**, ATR은 신호봉 i의 값 (L71, L76) | 동일 (신호봉 ATR[i]는 확정봉 값이라 §3-1의 BRK용 ATR[i−1] 교정 대상 아님 — 봉내 진입이 아니므로 인과성 문제 없음) | 일치 |
| 13 | 목표 | 2R: e + 2×(e−stop) / e − 2×(stop−e) (L72, L77) | 2R 유지하되 R의 기준 진입가는 실제 체결가(다음 봉 시가)로 재계산 | 교정 파생 (결정 명시 필요) |
| 14 | 청산 판정 순서 | 매 봉 스탑 먼저, 목표 나중 (elif) — 같은 봉 동시 도달 시 스탑 우선(비관) (L55-60) | 동일 | 일치 |
| 15 | 청산 체결가 | 스탑/목표 **레벨 그대로 체결** (갭 악화 없음) (L56-60) | 원본 유지가 기본값 — §3은 BRK에만 갭 악화를 명시. 갭 악화를 RSI에도 적용하려면 T0 전에 명세 결정 필요 | 일치 (미결 1건, 하단 참조) |
| 16 | 타임아웃 | 신호봉 인덱스 기준 i−ei ≥ 42 (4h×42=7일) → 그 봉 **종가** 청산 (L61-62) | 동일 기준점(신호봉) 유지 — 체결봉 기준으로 세면 1봉 오차 | 일치 (기준점 주의) |
| 17 | 진입 봉 청산 금지 | 진입 봉에서는 스탑/목표 미검사 (L72, L77의 continue/루프 종료) | 다음 봉 시가 체결로 구조가 달라짐: **체결봉부터 스탑/목표 검사 여부** T0 전 결정 필요 (터틀 same_day_stop 비관 전례 참조) | 교정 파생 (결정 명시 필요) |
| 18 | 비용 | 왕복 16bp를 거래 수익에서 일괄 차감 `done − 0.0016` (L26, L64) | 편도 8bp×2 (scalp_grid `COST_IN/COST_OUT=0.0008`) 명목 기준 — 왕복 16bp 동치 확인 (§4) | 일치 (표현만 변환) |
| 19 | 수익→사이징 변환 | 원본은 %수익 단순합 (사이징 없음) | 리스크 2%/포지션 스탑거리 역산 + heat/gross/일손실 정지 (§4) — 규칙이 아닌 실행 계약 계층 | 추가 계층 |
| 20 | 펀딩 | 원본 미반영 | 00/08/16 UTC 정산 시각별 보유 포지션에 적용 (§3-7) | 추가 계층 |

### 확정 사항 (2026-08-27, T0 가동 전·성과 조회 전 결정 — 사후선택 아님. 엔진 구현과 일치)
1. (#15) RSI-DIV 스탑/목표 청산은 **레벨 그대로 체결 (갭 악화 없음)** — 원본 유지.
   §3의 갭 악화는 BRK 봉내 스탑주문 모델에만 적용.
2. (#17) 다음 봉 시가 체결 이후 **체결봉부터 스탑/목표 검사 시작**, 같은 봉에서 스탑·목표
   동시 도달 시 **스탑 우선(비관)**, 이후 매 1h봉 검사.
3. (#13) 2R 목표 기준가는 실제 체결가(fill): **tgt = fill + 2×(fill − stop) = 3×fill − 2×stop**
   (숏은 대칭).

### 운영 명시 (판정·표시 관례)
- lab/tracke_null.py 의 최소 관측일 가드 MIN_DAYS=10 은 통계적 최소가 아니라 fail-closed
  운영 가드다 (10일 미만 원장으로 공식 판정 실행 불가).
- 판정 CLI 출력에서 '판정:' 줄의 허용 2문장만 해석 문구이며, 그 외 수치 진단
  (관측 최대·95% 상단·MC p·창 정보)은 참고 출력이다.
- 펀딩 부호 관례: 원장(tracke_ledger.csv)의 funding 이벤트 pnl 은 **양수=수취**(자본 증가분),
  대시보드 표의 펀딩 열은 엔진 상태 관례대로 **양수=지불** — 각 표면에 라벨로 명시돼 있다.

---

## 10. 부록 (2026-08-28 추가·사전 동결) — 빠른 익절 변형 E11·E12 (BRK24TP)

사용자 요청("스캘핑형 단타 — 짧게 들고 수익 챙기기")의 정직한 구현. 성과 조회 전 동결.

### 규칙 (동결 — 구현 확정 문구, 2026-08-28)
- 전략 BRK24TP = BRK24 와 진입·초기 스탑·사이징 **완전 동일(같은 코드 경로)**. 추가 규칙 2개:
  (a) **익절 1R**: tgt = fill + 1×(fill − stop) = **2·fill − stop** (롱/숏 공통식).
      봉내 레벨 체결(갭이 유리해도 레벨 — RSI-DIV #15 관례), 체결봉부터 검사(#17 관례).
      같은 봉에서 BRK 청산 레벨(스탑·역채널, 갭 악화)과 동시 도달 시 **BRK 청산 우선(비관)**.
  (b) **최대 보유 12×1h봉** — 진입봉=1, 존재하는 봉만 카운트(결측 봉은 카운트·청산 정지,
      fail-closed 관례), hold 12 도달 봉 종가 청산(원장 action "timeout").
      그 외 BRK24 청산 규칙(역채널 추적·갭 악화) 유지.
- **청산 우선순위 동결**: BRK 스탑/역채널 → 1R 목표 → 12봉 타임아웃. 청산 봉 종가 시각
  펀딩은 이미 청산된 포지션에 미적용(공통 관례).
- E11 = 바스켓 A / E12 = 바스켓 B (기존 동결 바스켓 재사용). 자본·리스크·비용 본 셀과 동일.
- **t0_variant = 2026-08-28 01:40:16 UTC (write-once·불변)** — 변형 서브상태의 최초 원자적
  기록 시각. 이전 봉은 본 팜 지표 상태의 깊은 스냅숏 상속으로 워밍업 대체, t0_variant 이전
  주문 생성은 구조적으로 불가.
- 러너 규약: 본 셀 재생·저장 완료 후에만 변형 실행, 변형 실패는 본 커밋에 무영향(격리),
  변형은 자체 last_ts 로 다음 실행에서 따라잡음. 폐지는 본 팜 폐지의 미러.
- 회귀 보증: E01~E10 원장·이력·상태가 변형 활성/부재와 무관하게 **바이트 동일** — 상시 테스트.

### 지위 (동결)
- 라벨: **"빠른 익절 변형 · 미검증 · 판정 권한 없음"**. 자본·승급 권한 영구 없음.
- **공식 판정(§6) 불변**: 공동 max-stat null 은 E01~E10 10셀 계약 그대로. E11·E12 는
  공식 판정 대상 아님(기술 보고만). 원장 분리: logs/tracke_variant_ledger.csv /
  tracke_variant_history.csv — 기존 tracke_ledger.csv 에 기록 금지.
- E01~E10 재생 결과 바이트 동일성(변형 추가 전후)을 테스트로 보증 — 본 실험 무영향.

### 사전 예측 기록 (결과 조회 전, 정직)
왕복 비용 0.16% 대비 1R 익절은 이익 폭을 자르고 손실 폭은 유지하므로, 비용 수학상
이 변형은 원본 BRK24 보다 **나쁠 것으로 예측**한다. 이 예측이 틀리는지 보는 것이
본 부록의 목적이며, 좋게 나와도 별도 사전등록 전방 검증 없이는 아무 권한이 없다.

---

## 11. 부록 (2026-08-28 추가·사전 동결) — 게이트·출판 변형 E13~E18 (그룹 v2)

사용자 요청("여러 조건 충족 시에만 진입" + "출판 시스템 편입")의 정직한 구현.
백테스트 기준선을 먼저 기록했고, **결과와 무관하게 편입**한다 (U1 절차).

### 셀 구성 (V2CELLS, 상태 variant2_cells, t0_variant2 write-once)
| 셀 | 전략 | 바스켓 | 라벨 |
|---|---|---|---|
| E13/E14 | BRK24GATE | A/B | 컨플루언스 게이트 변형 · 미검증 · 판정 권한 없음 |
| E15/E16 | BBMR (볼린저 평균회귀, 출판) | A/B | 볼린저 평균회귀 (출판) · 미검증 · 판정 권한 없음 |
| E17/E18 | RSI2 (Connors, 출판) | A/B | Connors RSI2 (출판) · 미검증 · 판정 권한 없음 |

### 규칙 (동결 — 원전 = lab/confluence_gate_test.py, lab/published_systems_test.py 1:1)
- **BRK24GATE**: BRK24 와 진입·스탑·사이징·청산 동일 코드 경로. 진입 허용 = 3중 게이트 AND
  (확정봉 [i−1]): ①close vs SMA200 (롱>/숏<) ②Wilder RSI14 vs 50 ③vol[i−1] > mean(vol[i−21..i−2]).
  NaN·미형성 전부 차단(fail-closed). 이중 돌파봉은 롱 우선 후 게이트.
- **BBMR**: BB(20, 2σ, ddof=0, SMA20 중심). 확정 종가<하단밴드 → 다음 봉 시가 롱,
  청산 확정 종가≥SMA20 → 다음 봉 시가. **롱 온리·스탑 없음** (출판 충실).
- **RSI2**: Connors 원전 — 롱 close>SMA200 ∧ RSI(2)<5, 청산 close>SMA5 / 숏 대칭(>95, <SMA5).
  Wilder 평활(dn=0 → 100, 무변동 50). **스탑 없음**.
- **사이징 (E15~E18, 스탑 부재)**: 포지션 명목 = equity × 1/3 (3슬롯 균등, 레버리지 없음).
  2% 스탑역산 사이징 미적용. **heat 정의(동결)**: 스탑 없는 포지션의 heat 기여 = 명목 × 5%
  (슬롯당 ≈1.667%) — 손실 상한이 아니라 진입 차단용 대리변수 (실질 제동 = 명목 1/3 ·
  3포지션 · 일손실 −5% 정지). gross 는 |u|×마크 통상 산입.
- **명시적 원전 차이(동결)**: ①같은 봉 청산 재진입 금지(팜 위상 규약 우선, lab 대비 최대
  1봉 지연 — BTC RSI2 전 기간 975건 중 1건) ②체결봉 종가는 BBMR/RSI2 청산신호 평가에
  포함(lab 1:1) ③이력 equity 열 = 기록 시점 변형 전 셀 합.
- t0_variant2 = 워밍업 수집(완결성 검증)까지 성공한 최초 원자적 기록 시각, write-once.
  기존 t0_variant(E11/E12)·본 팜 E01~E10 은 바이트 동일성 회귀 테스트로 무영향 보증.

### 사전 기준선 (2026-08-28 백테스트, 결과와 무관하게 편입 — 코드 lab/ 두 파일)
- **3중 게이트** (BRK24 대비, 3심볼 2021~2026, 왕복 16bp+펀딩): 거래 4,970→3,067 (−38%),
  승률 34.5→35.2%, **누적 +99.9% vs +96.3% (동률)**, MDD 52.7→39.1%. 게이트는 방향 정보를
  추가하지 못했고(원본 거래 71% 차단에도 수익 불변) 노출 축소로 MDD 만 낮췄다.
  연도 분포 상이 — 소수 연도 우연 가능성, 판정은 라이브 몫.
- **BBMR/RSI2 1h** (BTC/ETH, 왕복 16bp): 둘 다 **탈락 수준** — BBMR −0.165%/건(승률 62~64%),
  RSI2 −0.12~−0.18%/건(54~58%). 승률이 높아도 거래당 폭 < 비용. 원전 일봉에서는 BBMR 부호
  양(+0.26~+0.80%/건)이나 SE 가 평균을 압도(0과 구분 불가), RSI2 는 ETH 숏 꼬리손실.
  1h 적응은 봉 개수 보존 이식(SMA200 = 200일→8.3일)이라 원전 일봉 시스템의 반증 아님.
- **사전 예측 기록**: E15~E18 은 라이브에서도 비용 침식으로 음수 기대, E13/E14 는 E01/E02
  대비 거래 수 감소·수익 유사·낙폭 감소 기대. 좋게 나와도 별도 사전등록 없이는 무권한.

---

## 12. 부록 (2026-08-28 추가·사전 동결) — 전 유니버스 변형 E19~E21 (그룹 v3)

**전 유니버스 · 미검증 · 백테스트 기준선 없음(전방 전용) · 판정 권한 없음.**
- 유니버스 = T0(v3) 시점 봇 유니버스 관례(USDT 무기한, 24h 거래대금 ≥ $5M)의 상위 40종
  고정 코호트, 알파벳순 write-once 동결 (바스켓 A/B 중복 허용, 폐지·단절 시 슬롯 영구
  공석·교체 없음).
- E19 = BRK24, E20 = BRK24GATE(3중 게이트 v2 정의), E21 = MR — 기존 코드 경로 재사용.
- 셀당 신규 $10,000, 포지션당 리스크 **최대 1%**(스탑 역산, heat 잔여 클램프), 동시 최대
  6포지션, heat 6%(진입 승인 시점)·gross 10x·일손실 −5% 진입정지·펀딩 공통.
  같은 봉 신호 6개 초과 시 **심볼 알파벳순 선착** (처리 순회 순서의 사전 고정 —
  알파벳 앞 심볼 노출 편향 공시).
- **비용: 2단계 사전 고정 스트레스 모델 "major8_alt11"** — 메이저(BTC/ETH/SOL) 왕복 16bp,
  비메이저 왕복 22bp (편도 슬립 2→5bp). 캘리브레이션 추정치 아님 — 균일 16bp 가 얇은
  알트 비용을 낙관해 '비용 침식 관찰' 목적을 훼손하므로 보수 방향으로 고정 (Codex 합의).
  기존 셀(E01~E18)은 동결이라 소급 없음.
- 상태 variant3_cells / t0_variant3 write-once (40심볼 워밍업 완결 후 동결, 미달 시
  fail-closed 재시도). 원장·이력은 기존 변형 파일 통합(e19~e21 열, 스키마 이월).
- 러너 넷째 격리 단계 — v3 실패는 본·v1·v2 커밋에 무영향. E01~E18 바이트 동일성 회귀 보증.
- 공시된 한계: T0 당시 이벤트성 고거래대금 편향, 상장 420h 미만 심볼의 짧은 지표 이력,
  전방 전용이라 사전 예측 기준선 없음.
- **사전 예측 기록**: 코인 수 확장은 신호·비용을 함께 늘린다. E19 는 E01 대비 거래 빈도
  수 배·비용 침식 가속을 예측하며, "더 많은 코인 = 더 많은 수익"이 아님을 라이브로
  검증하는 것이 본 부록의 목적이다.

---

## 13. 무결성 교정 공시 (erratum, 2026-08-29) — 원장 재작성이 기존 행을 변조하고 있었다

**성과를 이미 조회한 뒤 발견·수정했음을 먼저 공시한다.** 아래는 전략·판정 규칙 변경이
아니라, §10~§12 가 단계별로 약속한 "바이트 동일" 격리 보증을 지키지 못하던 저장 경로의
전향적 교정이다. **이미 변조된 113행을 원래 비트로 복원하지 않는다** — 현재 파일 바이트를
그대로 기준선으로 동결하고, 앞으로의 추가 변조만 막는다. 소급 재계산·재작성 없음.

### 발견 경위
CI(Linux x86-64)에서만 회귀 테스트 2건이 실패했다 — 변형 원장의 E11 exit 행 pnl 이
1 ULP 어긋났다. macOS(arm64) 로컬은 통과했다.

### 원인 (실측)
`append_csv_atomic` 은 append 마다 파일 전체를 `pd.read_csv` → `to_csv` 로 재작성했다.
pandas 의 기본 `float_precision='high'`(precise_xstrtod)는 correctly-rounded 가 아니라
`to_csv` 가 쓴 최단 표기를 되읽을 때 ±1 ULP 어긋난다. 어긋나는 방향은 플랫폼·pandas
구현에 의존한다(원인을 FMA 등으로 단정하지 않는다 — 어셈블리 증거 없음).

결과는 두 가지다.
1. **기존 행 변조.** 커밋된 라이브 파일에서 유일키가 같은 기존 행이 실제로 바뀌었다:
   `logs/tracke_ledger.csv` **71건**, `logs/tracke_variant_ledger.csv` **42건**
   (예: E01/BTC/1787821200000/enter 의 qty `0.06690434152495012` → `0.0669043415249501`).
   이는 `keep="first"` 가 문서로 약속한 "재실행이 기존 이벤트를 덮지 못한다"의 위반이다.
   (드리프트는 매시 누적이 아니라 **각 행이 최초로 재작성될 때 1회 이동한 뒤 고정점**이다.)
2. **그룹 간 결합.** 한 실행에서 통합 변형 원장(VLEDGER)에 append 되는 횟수는
   `1 + (v2 활성) + (v3 활성)` 이다. 즉 E11 행이 겪는 재작성 횟수가 변형 그룹 활성
   여부에 따라 달라져, §10~§12 의 단계별 바이트 격리 보증이 저장 경로에서 깨졌다.
   (`1 + v2 + v3` 은 각 저장 단계까지 정상 도달한 실행에 한한다 — 초기화·중간 실패 실행은
   다르다.) 이번 차이는 **CSV 재작성 경로만으로 전부 재현됐고 계산 경로 차이는 관찰되지
   않았다** — 셀 로직·체결·사이징에서 결함은 발견되지 않았다.

### 수정 (실행 코드는 carrybot/aggressive/scalp_farm_runner.py 만 — 그 외 테스트·본 문서)
- `append_csv_atomic` / `_migrate_vhist_schema` 가 기존 파일을 `dtype=str`
  (`keep_default_na=False, na_filter=False`)로 읽어 **필드 문자열을 수치형으로 재해석하지
  않고** 옮긴다. 신규 행은 같은 `to_csv` 직렬화를 거쳐 문자열 공간에서 유일키를 비교하므로
  중복 제거 의미(keep first/last)는 그대로다.
- 부수 효과로 dtype 승격 사고(기존 `"100"` 이 신규 실수 행 때문에 `"100.0"` 이 되는 것)도
  사라진다.
- **보장 범위 (과장 금지)**: 이건 raw-byte copy-on-write 가 아니다. 보장은 `to_csv` 가
  만든 정규형 CSV(LF 개행, 불필요한 인용 없음, 결측은 빈 필드)에 한정된다 — 손으로 넣은
  인용부호·CRLF·BOM 은 재작성에서 정규화된다. Track E 의 네 CSV 는 전부 이 경로가
  생성·관리하는 정규형이다. 또 신규 행 직렬화가 이제 단독으로 일어나므로, 이론상 예전의
  concat dtype 강제와 다른 표기가 나올 수 있다(현 스키마의 float/int 계약에서는 실측상
  동일). pandas `to_csv` 표기 규칙 변화까지 막지는 못한다.
- **전략 코드(`scalp_farm.py`)·판정기(`lab/tracke_null.py`)는 손대지 않았다.** 판정기는
  동결 파일이고, 원장 바이트가 더 이상 변하지 않게 된 이상 그 파서 오차는 주어진 파일에
  대해 결정적이다. 다만 판정은 **Linux x86-64 / Python 3.11 환경에서 수행**하고 그때의
  pandas·numpy 버전을 함께 기록한다. 부트스트랩 경계가 1 ULP 수준으로 근접하면 판정을
  보류·공시한다.

### 검증
- 저장소에 커밋된 라이브 4개 CSV 전부: 빈 append·동일키 재append 후 **바이트 완전 동일**,
  신규 행 append 시 출력 = 기존 바이트 + 새 줄.
- 회귀 테스트 2건의 비교 대상을 pandas 왕복이 아니라 **원문 CSV 줄 바이트**로 교체
  (`ledger_lines_raw`) — 교정 전 코드에서는 macOS 에서도 실패한다(플랫폼 의존 제거).
- 신규 `TestLedgerByteImmutability` 7건 (헤더 전용 파일, keep='last' 이력 upsert 포함;
  교정 전 구현에서 4건 실패). pandas 2.3.3 / 3.0.5 양쪽 통과. 전체 1040 tests 통과.

### 남은 위험 (미처리, 권고만)
`requirements.txt` 는 `pandas>=2.0.0` 이고 트랙 워크플로는 매 실행 최신 pandas 를 설치한다.
위 교정으로 파서 의존은 제거됐지만 `to_csv` 표기 규칙 변화까지 막지는 못한다. Track E
워크플로만 pandas 를 좁게 고정하거나, 의존성 갱신 시 CI 재실행을 강제하는 편이 안전하다.

### 교정 시점 데이터 해시 (수정 직전, git 45533b8919c89f0911ec8b26f6e6b6e7e5ee8c1c)
| 파일 | SHA256 |
|---|---|
| logs/tracke_ledger.csv | 251240cb8df9e494eda1f87fc71c5f39bbdb6ef80f487fc48ce850fa06fce16a |
| logs/tracke_variant_ledger.csv | e7898fed7890c286d0abdf5913c464057c2d7bff1db9b718a1aec20a3f833b6e |
| logs/tracke_history.csv | 0d26f5bb80d4cd77df115a33e55613127ceeefa43a6ea6605d21fbffed08a559 |
| logs/tracke_variant_history.csv | fe20e27234a757b1836ddb64ec609633bc9041dabcc8f8227f856c3cf6ab735f |
| logs/tracke_state.json | 868eeab0077fd3d1a0b44bf652d49c3e8327d56f079f34c4e0d2e2197d376d98 |
